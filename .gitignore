# Python
__pycache__/
*.py[cod]
.venv/
venv/
.python-version

# Jupyter
.ipynb_checkpoints/

# Environment / secrets
.env
.env.*
!.env.example

# ---------------------------------------------------------------------------
# REAL COMPANY DOCUMENTS — NEVER COMMIT. This repo is PUBLIC.
# Internal EDR install/troubleshooting/operations docs describe tamper
# protection, exclusions, detection thresholds and console endpoints.
# Publishing them would expose the company's endpoint defenses.
# ---------------------------------------------------------------------------
corpus/private/
corpus/real/
*.private.*
private/

# Local model + index data
models/
qdrant_storage/
*.faiss

# Eval output (results committed deliberately, scratch is not)
eval/results/scratch/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# OS / editor
.DS_Store
.idea/
*.swp
